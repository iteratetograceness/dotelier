// TODO: animate

export async function Welcome() {
  return <p>Welcome!</p>

  // TODO: supabase user metadata
  // return (
  //   <p>
  //     Welcome
  //     {name ? `, ${name.length > 20 ? `${name.slice(0, 13)}...` : name}` : ''}!
  //   </p>
  // )
}
